---
- name: install packages
  apt: pkg={{item}} state=installed
  with_items:
    - git
    - vim
    - python-pip
    - python-dev
    - python-netaddr
    - libffi-dev
    - libssl-dev
    - gcc
    - git
#- name: install upgraded kernel
#  include: wily.yml
#  when: ansible_kernel != '4.2.0-35-generic'
